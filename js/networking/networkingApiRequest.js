class NetworkingAPIRequest {
  constructor(timestamp, validationCode, category, requestMsg) {
    this.timestamp = timestamp;
    this.validationCode = validationCode;
    this.requestMsg = requestMsg;
    this.category = category;
  }
}
