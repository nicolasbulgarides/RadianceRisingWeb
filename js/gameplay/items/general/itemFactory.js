class ItemFactory {
  constructor() {
    this.itemPresets = new ItemPresets();
  }

  attemptToCreateItemWithJustification(itemRequested, itemJustification) {
    let itemValidationResults = null;
  }

  attemptToCreateItemBypassJustification(itemRequested) {}
}
