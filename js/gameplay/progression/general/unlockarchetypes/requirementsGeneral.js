/**
 * RequirementsGeneral
 * Base class for all composite unlock requirements.
 * Provides a shared structure for managing common unlock criteria.
 */
class RequirementsGeneral {
  constructor() {
    // Initialize the common container for unlock requirements.
    this.requirements = [];
  }
}
