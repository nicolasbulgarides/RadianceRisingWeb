/**
 * Manages the display of all reward-related UI elements in the game.
 * Coordinates with specific reward type managers (like MojoUIManager) to present cohesive reward feedback.
 */
class RewardUIManager {
  /**
   * Creates a new instance of the reward UI manager.
   * Initializes the UI management system for all types of rewards.
   */
  constructor() {
    this.rewardUIManager = new RewardUIManager();
  }
}
