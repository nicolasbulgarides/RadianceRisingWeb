class AchievementUIManager {
  constructor(achievementPresets) {
    this.achievementPresets = achievementPresets;
  }

  generateBlankAchievementUI() {}
  generatePlayerFilledAcheivementUI(playerSaveComposite) {
    let allPlayerAchievements = playerSaveComposite.achievementRecords;
  }
}
