import AchievementCategory from "../achievementCategory.js";

/**
 * ItemAchievements represents achievements tied to items.
 */
class ItemAchievements extends AchievementCategory {
  constructor(categoryName) {
    super(categoryName);
    this.populateAllAchievements();
  }

  /**
   * Initializes and populates all item achievements.
   */
  populateAllAchievements() {
    // TODO: Implement logic to populate all item achievements.
  }
}
