import AchievementCategory from "../achievementCategory.js";

/**
 * ConstellationAchievements represents achievements related to constellations.
 */
class ConstellationAchievements extends AchievementCategory {
  constructor(categoryName) {
    super(categoryName);
    this.populateAllAchievements();
  }

  /**
   * Initializes and populates all constellation achievements.
   */
  populateAllAchievements() {
    // TODO: Implement logic to populate all constellation achievements.
  }
}
