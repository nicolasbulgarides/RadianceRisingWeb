/**
 * CompletionAchievement represents achievements for players who complete every challenge.
 */

class CompletionAchievements extends AchievementCategory {
  constructor(categoryName) {
    super(categoryName);
    this.populateAllAchievements();
  }
  /**
   * Initializes and populates all completion / game progress and milestone achievements.
   */
  populateAllAchievements() {
    // TODO: Implement logic to populate all completion achievements.
  }
}
